# Dependabot disabled - major version bumps were causing CI failures
# Re-enable selectively when ready to update dependencies manually

version: 2
updates: []

# Previous configuration (for reference):
# - cargo (Rust) - weekly, 5 PRs max
# - npm /desktop - weekly, 5 PRs max
# - npm /extension - weekly, 5 PRs max
# - gradle /android - weekly, 5 PRs max
# - github-actions - weekly, 5 PRs max
